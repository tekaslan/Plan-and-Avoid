#include "../include/airtraffic.h"

int main()
{
    // struct airCorridor *corridors = (struct airCorridor *) malloc(sizeof(struct airCorridor));
    // readAirCorridors(corridors);

    // struct Pos pos;
    // pos.lat = 40.794602052462118;
    // pos.lon = -73.37162950399329;
    // pos.alt = 1990.330610906015;

    // double const d_buffer = 0.5;    // [NM]
    // double cost = airCorridorOccupationCost(&pos, corridors);


    

    return EXIT_SUCCESS;
}